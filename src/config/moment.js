const calendarStrings = {
    lastDay: '[Dün saat] LT',
    sameDay: '[Bugün saat] LT',
    nextDay: '[Yarın saat] LT',
    lastWeek: 'dddd [saat] LT',
    nextWeek: 'dddd [at] LT',
    sameElse: 'L'
};

const animeDateFormat = "DD.MM.YYYY"
const mangaDateFormat = "DD.MM.YYYY"

export { calendarStrings, animeDateFormat, mangaDateFormat }